{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Deposito list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombrealmacen</th>
                <th>Cantmaterialdisponible</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('deposito_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombrealmacen }}</td>
                <td>{{ entity.cantmaterialdisponible }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('deposito_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('deposito_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('deposito_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
