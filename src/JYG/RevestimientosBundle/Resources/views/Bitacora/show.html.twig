{% extends 'JYGRevestimientosBundle::baseAdmin.html.twig' %}

{% block body -%}
    <div class="row">
    <div class="page-header">
        <div class="col-sm-8"><h1>Operación /<small>#{{ entity.id }}</small></h1></div>
        <div class="col-sm-4"><a id="volver" href="{{ path('bitacora') }}" class="btn btn-primary pull-right" type="button"><span class="glyphicon glyphicon-share-alt" aria-hidden="true"></span>  Regresar a la Bitácora</a></div>
    </div>
    </div>

    <div class="row">
        <div class="col-lg-12 col-sm-12 col-xs-12">
            <div class="panel panel-default">
                <div class="panel-heading">Resumen de la Operación</div>
                    <table class="table table-striped table-bordered record_properties">
                        <tbody>
                            <tr>
                                <th>ID</th>
                                <td>{{ entity.id }}</td>
                            </tr>
                            <tr>
                                <th>Usuario</th>
                                <td>{{ entity.login }}</td>
                            </tr>
                            <tr>
                                <th>Operación Realizada</th>
                                <td>{{ entity.operacion }}</td>
                            </tr>
                            <tr>
                                <th>Fecha</th>
                                <td>{{ entity.fecha|date('d-m-Y -- g:ia') }}</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
